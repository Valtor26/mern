const catme = require('cat-me');

console.log(catme())


// all the code of the installed package will be stored in node modules

// package.json --> it maitains all the dependencies package of your code

// package-lock.json ---> it maintains all the dependency chain for every package